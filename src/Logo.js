import React from 'react';
import logo from './logo.svg';

function Logo(props) {
    return (
        <div>
            <img src={logo} alt='logotype' />
        </div>
    );
}

export default Logo;